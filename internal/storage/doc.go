// Package storage is the persistence layer: it reads and writes links to
// MySQL via database/sql. This is the repository (≈ Eloquent/Repository in
// Laravel). Nothing above this layer should know SQL.
package storage
